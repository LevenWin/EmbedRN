# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'InsertRNTest' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!
pod 'Yoga', :path => './RNComponents/node_modules/react-native/ReactCommon/yoga'
pod 'React', :path => './RNComponents/node_modules/react-native', :subspecs => [ 'Core',
'RCTImage',
'RCTNetwork',
'RCTText',
'RCTWebSocket',
# 添加其他你想在工程中使用的依赖。
]
  # Pods for InsertRNTest

  target 'InsertRNTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'InsertRNTestUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
